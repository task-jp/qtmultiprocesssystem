import QtQuick 2.15
import QtQuick.Layouts 1.15
import QtQuick.Controls 2.15

import '../common/'

AbstractMain {
    id: root

    Label {
        anchors.centerIn: parent
        text: 'Music'
        font.pixelSize: 64
    }
}
