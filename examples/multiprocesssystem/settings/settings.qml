import QtQuick 2.15
import QtQuick.Controls 2.15
import '../system'

AbstractMain {
    id: root

    Label {
        anchors.centerIn: parent
        text: 'Settings'
        font.pixelSize: 64
    }
}
